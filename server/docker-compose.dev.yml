services:
  db:
    image: postgres:16-alpine
    container_name: arrowthing-dev
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - arrowthing-dev-data:/var/lib/postgresql/data

volumes:
  arrowthing-dev-data:
